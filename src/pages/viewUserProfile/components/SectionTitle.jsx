
const SectionTitle = ({ children }) => {
    return (
        <div>
            <h3 className="text-xl font-semibold text-[#58001C] mb-3">{children}</h3>
        </div>
    );
};

export default SectionTitle;
